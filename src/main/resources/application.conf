akka {
  actor {
    provider = remote
    allow-java-serialization = on
  }
  remote {
    artery {
      transport = tcp
      canonical.hostname = "127.0.0.1"
      canonical.port = 25520
    }
  }
}